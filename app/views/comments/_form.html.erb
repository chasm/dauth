<%= form_for [@article, @comment] do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.text_area :content, :class => 'span6', :style => "height: 120px", :placeholder => "Add your comment here." %>
  </p>
  <p>
    <%= f.text_field :site_url, :class => 'span4', :placeholder => "URL of your website" %>
  </p>
  <p>
    <%= f.text_field :author_name, :class => 'span3', :placeholder => "Your name" %>
  </p>
  <p class="actions">
    <%= f.submit :class => "btn btn-warning" %>
  </p>
<% end %>
